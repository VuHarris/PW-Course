// Initialize a const variable named `number` and assign it the value of `12`
const number = 12;

// Initialize a string variable named `name` with value of your number
let name = "my number";

// Initialize a boolean variable name `isEven` an assign it the value of `false`
let isEven = false;

// Check if the numver is even, re-assign the value of `isEven` to `true`
if (number % 2 === 0) {
    isEven = true;
} 
// Log the value of `isEven` to the console
console.log(isEven);

