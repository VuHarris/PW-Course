// Check the block of code and fix the errors in it
const myName = "Alex";
myName = "Nagi";
console.log(myName);

/* The code above has an error because it tries to re-assign a new value to a variable declared as `const`. 
In JavaScript, `const` variables cannot be re-assigned after they have been initialized. */

let myName = "Alex";
myName = "Nagi";
console.log(myName);